import React from "react";

export const metadata = {
    title: 'Terms &amp; Conditions',
}

export default function LocalLayout({ children, params }) {
    return (
        <>
            {children}
        </>
    )
}